# API Security
# Set this to require X-API-Key header on all requests (except /health)
# Leave empty to disable authentication
API_KEY=your-secret-api-key-here

# NVIDIA NIM Configuration
NIM_API_KEY=your_nvidia_nim_api_key_here
NIM_BASE_URL=https://integrate.api.nvidia.com/v1
NIM_MODEL=nvidia/nv-embedqa-e5-v5
NIM_RPM_LIMIT=40
NIM_EMBEDDING_DIM=1024

# Data directories
DATA_DIR=/home/claraverse/ClaraVector/data

# Server configuration
HOST=0.0.0.0
PORT=8000
WORKERS=2

# Processing limits
MAX_FILE_SIZE_MB=10
MAX_CHUNK_SIZE=512
CHUNK_OVERLAP=50

# Queue settings
MAX_RETRIES=3
RETRY_DELAY_SECONDS=5.0

# CORS Configuration
# Use "*" to allow all origins (development only!)
# For production, specify allowed origins comma-separated:
# CORS_ORIGINS=https://myapp.com,https://admin.myapp.com
CORS_ORIGINS=*
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=*
CORS_ALLOW_HEADERS=*
